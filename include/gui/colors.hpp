#pragma once
#include <raylib.h>

namespace colors {
    static constexpr Color anthraziteGrey = {200, 200, 200, 179}; //70% opacity
    static constexpr Color white = WHITE;
    static constexpr Color black = BLACK;

    static constexpr Color transparent = BLANK;

    static constexpr Color warning = RED;
} // namespace colors
